name: key-value-server

dependencies:
  - servant-server

ghc-options:
  - -Wall
  - -Wno-name-shadowing

tests:
  specs:
    main: Spec.hs
    source-dirs:
      - test
      - src
    dependencies:
      - base
      - bytestring
      - hspec
      - http-client
      - http-types
      - lens
      - servant
      - wai
      - warp
      - wreq
      - containers
      - string-conversions
      - text
