name: key-value-server

dependencies:
  - servant-server

ghc-options:
  - -Wall
  - -Wno-name-shadowing

tests:
  specs:
    main: Spec.hs
    source-dirs:
      - test
      - src
    dependencies:
      - base
      - bytestring
      - containers
      - hspec
      - http-client
      - http-types
      - lens
      - servant
      - string-conversions
      - text
      - wai
      - warp
      - wreq
